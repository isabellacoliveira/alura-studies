// vamos pegar o itarefa e colocar aqui 
export interface ITask {
    task: string,
    time: string,
    selected: boolean, 
    completed: boolean,
    id: string
} 
// vamos dar export nesse iterface 